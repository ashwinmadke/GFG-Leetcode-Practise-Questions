// https://leetcode.com/problems/counting-bits/description/

/************************************************************ Brute Force Approach ************************************************/
Using Built-in function Integer.bitCount()

TC - O(n)
SC - O(n)

public int[] countBits(int n) {
        int[] result = new int[n+1];

        for(int i=0; i<=n; i++){
            result[i] = Integer.bitCount(i);
        }
        return result;
    }
